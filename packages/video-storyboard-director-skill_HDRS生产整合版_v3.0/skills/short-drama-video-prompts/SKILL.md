---
name: short-drama-video-prompts
description: 把已确认短剧分镜、关系职责、情绪余波与冻结关键帧编译成可直接复制的文生或图生视频提示词，也可按要求写时间线配乐意图。用户提到Seedance、Kling、Runway、Veo、Sora、人物动作表演、运镜、口型、声音、环境运动、起止状态或分镜转视频提示词时使用；不生成媒体，不改分镜边界、剧情或台词。
license: MIT
---

# 短剧视频与时间线音乐提示词

把已确认镜头和起始帧写成有边界、可生成、可直接复制的 `剧集/<EP>/视频提示词.md`。
每镜使用 `MOTION-...` 标题并引用对应 `SHOT-...`。

## Quick Start

```text
用 $short-drama-video-prompts 把 EP001 分镜逐镜写成可直接复制的视频提示词
```

## 入口与边界

有当前分镜、冻结关键帧和必要视觉事实即可开始；图片提示词不是前置门槛。运动提示词拥有从已确认
起点到终点的变化：表演动作、道具状态、运镜、节奏和声音提示。先按分镜的「输入参考图」选择路径：

- 有可读的真实 `REF-...` 图片时是**图生视频**；原样携带分镜中的槽位、顺序、项目相对路径、
  中文名称、控制与不得控制范围，静态身份、造型、地点和构图可由参考帧与视觉设定共同拥有。
- 「输入参考图：无」时是**文生视频**；视频文档必须写非空「静态视觉锚点」，且把它原样纳入
  可复制正文，让提示词本身交代人物、造型、地点、构图和光线中本镜必要的事实。不得只写
  `preserve/keep/保持已有外观`，因为执行端没有收到可供保持的图片。

创作者可读说明跟随项目语言；可复制正文跟随 `short-drama.json#/format/prompt_language`。没有
项目配置时，优先服从用户本次指定语言；仍未指定时跟随用户交互语言，不自行切换为英文，并在同一任务中保持一致。

## 工作流

1. 原样抄入分镜的「输入参考图」，据此标明「生成方式：图生视频」或「文生视频」。
2. 写不能动的边框：图生视频以参考帧和视觉设定锁定；文生视频把必要静态事实写入「静态视觉锚点」和可复制正文。
3. 确定本镜在情绪流中的唯一职责、唯一主要变化和可验证终点；继承上一镜余波，不让人物无因恢复中性或重复同一冲击。
4. 写“静态锚点 → 起点/余波 → 触发 → 主体动作 → 次级反应 → 运镜 → 声音 → 终点”，必要时用秒数分段。
5. 只在真实参考帧已经说明静态外观时删除重复描述，并保留防漂移约束。
6. 检查不跨下一镜、不要求互斥动作、不让人物或道具瞬移变形。
7. 继承物理母状态、关键道具状态和声音时间线；切换到反应镜、特写或空镜不能让持续环境清零。
8. 用户要整集就完成整集，镜头组只是内部批次。

## 情绪转译子技能（按需）

当分镜已经通过 `$short-drama-emotion-translate` 确认情绪预算、主峰值、行为链和运镜触发时，本阶段只做执行降维：继承已确认动作与余波，不重新抬高情绪等级，不新增哭喊、摔物、逼近、第二峰值或额外运镜。这是“禁止二次放大”。

当用户直到视频提示词阶段才明确提出“表演太平、强化情绪、微表情和肢体、运镜配合情绪”，且上游没有情绪设计时，可调用同包的 `$short-drama-emotion-translate` 做最小补充，但不得改变既定镜头边界、起点、终点、Dialogue、VO、道具状态或下一镜Starting State。

情绪设计投影到单镜时，强制压缩为：一个主要动作变化、最多一个次级反应、一个主要运镜、持续环境状态和一个可验证终点。复杂行为链拆到既有后续镜头，不在一个2至3秒镜头内重演完整情绪过程。

商业竖屏默认单镜如无必要不超过3秒：常规叙事镜头约1至3秒，快切反应镜头约0.5至1.5秒，空镜或情绪镜约1至2秒。超过3秒必须继承分镜已经确认的必要性；本阶段不能自行延长，只能用长台词、连续动作、空间关系、情绪停顿、关键调度或有意长镜头设计所需的镜内注意变化把已确认时长写清。

## 提示词要求

- 从可见起点开始，以可验证终点结束。
- 主体、动作对象、方向、幅度、速度和先后关系明确。
- 表演写动作与反应，不写无法执行的抽象结果。
- 已确认的情绪峰值只执行一次；提示词不得为了“更抓人”擅自升级动作幅度或改变对手权力状态。
- 当前镜头只执行其在“刺激、接收、选择、反作用、余波”中的职责，不重复前镜已经完成的刺激或本能反应。
- 运镜与人物动作不争夺注意力；复杂运镜必须有叙事理由。
- 声音只写该镜需要的对白、画外音、环境或转场职责，不擅自补歌词。
- 正文可直接复制，不含占位符、流程说明、文件路径或 QA 结论。
- 用户要求可直接投喂时，只给正式提示词正文和必要禁止项，不附检测警示、规则解释或内部评分。
- 可复制视频提示词正文不使用连续两个半角连字符；时间段使用“0至2秒”或自然语言。
- VO播放时画内人物闭口表演；没有台词且未要求冻结的人物保留一个克制、可见且有心理原因的活人反应。

## 按需知识

默认只读本 SKILL、当前分镜和必要视觉事实。遇到对应问题时只打开一份：

- 阶段边界与规则分级：[阶段契约](references/stage-contract.md)
- 单镜运动正文的最小配方：[运动配方](references/motion-recipe.md)
- 外部执行需要的明确动作语法：[生产提示词语法](references/production-prompt-grammar.md)
- 多人表演、口型和精确时序：[表演与动作时序](references/performance-action-timing.md)
- 商业情绪流的跨镜继承与单镜降维：[情绪流执行](references/emotion-flow-execution.md)
- 使用Seedance或需要中文直出时：[Seedance中文执行档](references/seedance-chinese-execution-profile.md)
- 生成失败或成片不可剪时：[失败诊断与单变量修复](references/qa-and-minimal-repair.md)
- 运镜、声音和相邻镜头连续性：[摄影与声音连续性](references/camera-audio-continuity.md)
- 动作负载、形变和生成风险：[可生成性](references/generability.md)
- 多镜容器或静态漫剧的交付方式：[交付形态](references/delivery-profile.md)
- 完成前的边界检查：[审查与示例](references/review-and-fixtures.md)

用户明确要求时间线音乐时，在本文档增加独立章节，写使用区间、剧情功能、进入/退出、动态曲线和
禁用项；歌词必须来自用户提供或明确接受的文本。静态漫剧只需要关键帧切换与配音时可以跳过逐镜
视频运动提示词，直接进入生产预览。

## 完成与投产

每个点名镜头都有明确起点、主变化、时序、运镜/声音边界和可验证终点，且与相邻镜头连续，即完成。
交付前还应确认持续物理状态未清零、关键道具没有重复动作、声音身份与口型一致、正文符合目标模型与语言档。
实际生成转 `$short-drama-produce`，先展示精确 job 并取得显式确认；本技能不调用外部服务。
五份创作文档齐备后，可转 `$short-drama` 对跨文档结构做一次机械核对；内容质量仍由创作者审查。

## 安装维护

只有安装、升级或排障时运行 `python3 scripts/selftest.py`。
